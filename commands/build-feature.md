---
description: Run the full design-to-delivery pipeline — brainstorming, planning, and verified execution
---

Run the build-feature skill to take a feature request from idea to verified implementation. Chains the brainstorming, plan-writing, and plan-execution workflows into a single pipeline with gates at every transition. User confirms only at gates — everything else runs automatically.
